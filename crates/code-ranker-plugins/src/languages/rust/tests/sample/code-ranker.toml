version = "5.0"
# Self-contained config for the code-ranker "rust" sample fixture.
# Pin the plugin and KEEP test files in the graph (ignore.tests = false) so the
# fixture is reproducible regardless of any repo-level config and so that test
# files' imports stay visible in the report.
[plugins]
enabled = ["rust"]
[plugins.base.ignore]
tests = false
