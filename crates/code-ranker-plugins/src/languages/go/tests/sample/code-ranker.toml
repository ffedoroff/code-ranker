version = "5.0"
# Self-contained config for the code-ranker "go" sample fixture.
# Pin the plugin and keep test files in the graph (ignore.tests = false) so the
# fixture is reproducible regardless of any repo-level config.
[plugins]
enabled = ["go"]
[plugins.base.ignore]
tests = false
